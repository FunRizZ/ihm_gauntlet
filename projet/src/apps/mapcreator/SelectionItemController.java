package apps.mapcreator;

import javafx.fxml.FXML;
import javafx.scene.control.Button;
import javafx.scene.layout.HBox;

public class SelectionItemController extends HBox {
    @FXML
    Button Wall;

    public SelectionItemController(){
//        Wall = (Button) this.getChildren().getFirst();
//        Wall.setOnAction(event -> System.out.println("a"));
    }
}
